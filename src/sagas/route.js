// import { fork, take, put, call } from "redux-saga/effects";
// import history from "../history";

// function* redirect() {
//   while (true) {
//     const { path } = yield take("saga-redirect");
//     history.push(path);
//   }
// }

// export default [fork(redirect)];
