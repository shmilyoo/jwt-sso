// import { type as accountType } from "./account";

const initState = {
  // isLoading: false
};

export default (state, action) => {
  switch (action.type) {
    case 'value':
      break;

    default:
      break;
  }
};
